# ============================================================
# pizza_assorted — requirements.txt
# Python 3.10+
# ============================================================

# --- Этап 2: Extractors ---

# PDF
pymupdf>=1.23.0          # fitz — быстрый основной бэкенд
pdfplumber>=0.10.0       # резервный бэкенд, лучше для сложной вёрстки

# DOCX
python-docx>=1.1.0

# CSV / Parquet
pandas>=2.0.0
pyarrow>=14.0.0          # parquet + ускоритель pandas

# OCR (изображения)
pytesseract>=0.3.10      # обёртка над системным tesseract-ocr
Pillow>=10.0.0           # PIL — загрузка и предобработка изображений
easyocr>=1.7.0           # опциональный резервный OCR-бэкенд

# HTML
beautifulsoup4>=4.12.0
lxml>=5.0.0              # быстрый парсер для BeautifulSoup

# Определение кодировки
chardet>=5.2.0

# --- Этап 3: Detectors ---

# (нет внешних зависимостей — только re, dataclasses, typing из stdlib)

# --- Общие утилиты ---

tqdm>=4.66.0             # прогресс-бары в CLI
colorlog>=6.8.0          # цветной вывод логов

# --- Опционально (для разработки) ---
# pytest>=8.0.0
# pytest-cov>=5.0.0