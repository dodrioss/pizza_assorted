# ========================================
# Основные зависимости проекта PDN Scanner
# ========================================

# PDF обработка
pymupdf>=1.23.0          # fitz — быстрый основной бэкенд
pdfplumber>=0.10.0       # резервный бэкенд, лучше для сложной вёрстки

# DOCX
python-docx>=1.1.0

# CSV / Parquet / Табличные данные
pandas>=2.0.0
pyarrow>=14.0.0          # parquet + значительное ускорение pandas

# OCR (изображения) — усиленная версия
pytesseract>=0.3.10      # обёртка над системным tesseract-ocr
Pillow>=10.0.0           # PIL — загрузка и предобработка изображений
easyocr>=1.7.0           # опциональный нейросетевой OCR
#paddleocr>=2.8.0         # ← Добавлено: самый мощный OCR для сложных скриншотов
#paddlepaddle>=2.6.0      # ← Обязательно для PaddleOCR (CPU версия)

# OpenCV — критично для улучшенной предобработки изображений
opencv-python-headless>=4.8.0   # headless версия (без GUI, быстрее и легче)

# HTML
beautifulsoup4>=4.12.0
lxml>=5.0.0              # быстрый парсер для BeautifulSoup

# Определение кодировки
chardet>=5.2.0

# CLI и утилиты
tqdm>=4.66.0             # прогресс-бары
colorlog>=6.8.0          # цветной вывод логов

# Системные утилиты
psutil>=5.9.0            # для log_memory_usage

# ========================================
# Опционально (для разработки и тестов)
# ========================================
pytest>=8.0.0
pytest-cov>=5.0.0
black>=24.0.0
isort>=5.13.0
flake8>=7.0.0

rich
pdf2image